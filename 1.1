#include <iostream>
using namespace std;

struct PhanSo {
    int tu, mau;
};

int main() {
    PhanSo p;
    cout << "Nhap tu so: ";
    cin >> p.tu;
    cout << "Nhap mau so: ";
    cin >> p.mau;
    cout << "Phan so vua nhap la: " << p.tu << "/" << p.mau << endl;
    return 0;
}
